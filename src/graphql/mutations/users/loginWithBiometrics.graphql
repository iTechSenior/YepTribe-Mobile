mutation loginWithBiometrics($uuid: String!) {
  loginWithBiometrics(uuid: $uuid) {
    user {
      id
      email
      firstName
      lastName
      username
      phone
      roles
      uuid
    }
    token
  }
}
